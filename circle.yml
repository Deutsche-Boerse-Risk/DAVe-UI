machine:
  services:
    - docker

dependencies:
  pre:
    - npm install -g grunt-cli
    - npm install -g typescript
  override:
    - npm install

test:
  override:
    - grunt dist

deployment:
  staging:
    branch: master
    commands:
      - ./dockerfile/create_docker.sh
      - ./deployment/kube_deploy.sh
